# 页面性能优化记录 · 2026-09-06

## 可复核结果

测量同一项目的 `next build` 静态生产产物，枚举 `out/index.html` 的初始 JavaScript 脚本，去重后累加文件字节数。gzip 是本地逐文件压缩估算，不是实际服务器传输量。

| 指标 | 本轮修改前 | 修改后 | 变化 |
| --- | ---: | ---: | ---: |
| HTML 初始 JS 原始字节 | 1,420,957 | 734,946 | -48.3% |
| HTML 初始 JS gzip 估算字节 | 428,894 | 223,937 | -47.8% |
| 初始脚本数量 | 11 | 9 | -2 |

基线在本轮开始、改动前读取上一轮成功构建的 `out/index.html`；修改后重新构建。不是对全部异步 chunk 求和，也不是浏览器加载耗时或模型生成速度对比。进入对话后会按需下载公式库，因此不宣称整个解题会话的总流量减少同等比例。

复核命令：`npm run build && npm run test:performance`。新增预算为初始 JS ≤850,000 bytes，gzip 估算 ≤260,000 bytes；超出时返回非零退出码。预算脚本列出逐文件数据，方便定位后续回退。

## 本轮实施

1. Markdown/KaTeX 从首页初始依赖中拆出，开始对话即预加载，与等待模型并行；保留真实公式渲染与数学复制结构。异步加载慢或失败时保留原文及操作按钮，不让整个对话消失。
2. 板书编排依赖改为按需导入，未打开板书时不加载其数学处理链。恢复旧板书、接收结果和重新打开的异步路径均检查会话代次，防止切题后旧任务写回。
3. 历史消息列表独立 `memo`，输入框草稿、滚动提示等局部状态变化不再重新遍历并创建全部历史消息元素；保留原有逐消息 `memo`，未采用可能破坏任意选字、滚动定位和 PDF 的激进虚拟列表。
4. Markdown 正文解析独立记忆；尾部状态/复制反馈变化不重做未变化正文的 Markdown 与公式解析。不按段落盲拆流式 Markdown，避免破坏列表、跨段公式和代码块。
5. 猜你想问的滚动、resize、ResizeObserver 回调合并至下一动画帧；没有建议内容时不安装该组监听与观察器，卸载取消待执行帧。
6. 原有 40ms 文本合并、350ms 延迟持久化、裁剪/手写/PDF 按需加载保留。新增 2,000 碎片压力测试：顺序完整、无丢字、更新次数不超过 51 次。

## 验证范围与边界

- 725 个自动化测试、48 个测试文件通过；lint、typecheck、生产构建和体积预算通过。
- 新增真实 React 异步边界测试，等待完整渲染后检查正文、KaTeX 和尾部状态。原有静态 markup 测试只替换加载边界，仍使用真实正文渲染器；不以这些同步测试代替浏览器验证。
- 浏览器实际路径：本地首页 → 相册原图 → 裁剪 → 模型识别与含公式讲解 → 关键填空 → 显示答案 → 继续 → 下一步讲解与操作按钮。首轮继续测试恰逢开发热更新完整刷新而被中断，不作为成功样本；代码稳定后完整重跑成功。
- 回复后实际向上滚动成功（scrollTop 从2236.5变为1392.5）；PDF 预览包含当前对话和公式，关闭后正常返回；开始新题回到首页，没有残留的新讲解提示或草稿。
- 裁剪覆盖：真实四角拖拽/方向键、恢复全图、旋转、放大到150%、返回，以及320×740、390×844和844×390布局；未改变图像比例或识别算法。
- 未提供低端真机、弱网条件下的 LCP/INP/TBT 前后对照，也未测得模型端延迟改善；不宣称帧率、请求耗时或整题速度提升48%。后续若做这类目标，需要独立性能采样，不应从构建体积推算。

## 稳定性约束

没有调整模型、提示词、答案校验、业务状态机、会话密钥、缓存隔离和已有学习入口。没有修改全局滚动/触摸策略；没有取消浏览器文本选择或减少数学内容。裁剪 UI 是用户另行选择的浅色方案，不计作性能收益。
