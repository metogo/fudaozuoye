# 公式显示治理 · 2026-09-06

## 范围

目标是改善模型内容的格式保真、可读性和失败反馈，不是证明模型的数学推理正确。没有更改答案、评分或学习状态机，也没有增加每轮模型调用。

### 输出端

- 对话、完整讲解、知识诊断共用公式输出规范；步骤填空生成请求也携带该规范。
- 明确变量下标、指数范围、括号及定界符配对；完整表达式不能被中文拆开；较长恒等式和连续推导独立展示，允许 aligned。
- 这是输出约束，不保证模型每次遵守；前端仍保留兜底。

### 解析与异常处理

- 普通正文与答案槽复用保守的裸 LaTeX 片段识别，覆盖中文旁的关系式和嵌套分式/根式。已有定界符、代码、普通文字及未知命令不被擅自改写。
- 对同一段公式内同时出现 x1 和 x_{1} 这样的混用给出折叠提示；不推断 x1 一定代表下标。该检测只覆盖常见数字下标，不是全题符号一致性证明。
- 流式输出尚未闭合的 `$`、`$$`、`\(`、`\[` 片段暂时显示“公式正在补全…”，闭合后正常渲染。原始消息和复制数据不变。结束后仍未闭合则提示核对。
- KaTeX 语法失败保留原表达式并标记“公式格式需核对”，不暴露解析器堆栈，不静默给出猜测修复后的数学式。保留失败原文意味着异常时仍可能看到有标记的公式代码。
- 保留 MathML、LaTeX annotation 与复制结构；关闭可信 HTML/外部资源能力，限制宏展开和尺寸。

### 屏幕与导出

- 每个公式作为完整单元排版，不在同一表达式的括号、上下标之间随正文折行。超长单元限制在内容宽度内，可键盘/横向滚动查看。
- 对长度较大或含连续等式的公式使用独立行；增加斜体末端和上下标所需留白。降低独立公式过强的字号。
- PDF 使用同一解析组件，包括已展示的填空答案。屏幕预览保留横向查看；打印时在 KaTeX 允许的运算符断点换行，不在纸面使用滚动裁切。

## 验证证据

- 全量774项测试、50个测试文件通过；类型检查、lint、前后端构建通过。首屏初始 JS 为735,748字节、gzip估算224,343字节，仍通过既有性能预算；格式治理没有把 KaTeX 重新放回首页初始包。
- `tests/math-quality.test.tsx`：上下标、判别式、嵌套分式、根式、物理单位、化学式、aligned、cases、超长求和、中文旁裸片段、混用下标、异常语法、每一个流式切片、PDF 填空以及外链/图片安全。
- `MATH_VISUAL_QA=1 npx vitest run tests/math-quality.test.tsx` 可生成 `outputs/math-qa/` 的验收样例，复用真实 RichLearningText、ConversationDocument 及项目样式；是确定性渲染验收，不是实时模型端到端成功率统计。
- 浏览器设置 320×740、390×844（系统滚动条占15px），页面 scrollWidth 均等于可用 clientWidth；所有样例公式 scrollHeight 不大于 clientHeight，未发现竖向裁切。
- 超长公式宽499px、容器335px，按向右键后 scrollLeft 从0变为40，内容可查看；展开符号写法核对提示成功。
- PDF 预览在320px无页面横向溢出；794px纸张近似宽度下13个公式单元全部容纳。尚未进行系统打印生成 PDF 的分页验收，不能把预览等同于最终打印件验证。
- IAB 截图出现与 DOM 尺寸不一致的缩放/拼接现象，截图仅辅助留档；布局结论主要依据浏览器实时几何数据和键盘操作，不声称像素级一致。

## 保留的边界

- 无法自动证明公式语义、运算步骤或原题识别正确；模型一开始就把变量写错而没有其他上下文对照，可能无法发现。
- 未实现任意 LaTeX 方言、所有宏和所有混合文本的自动修复；不确定时宁可保留原文并核对。
- 极宽且不可分割的矩阵/单一结构仍需专项打印适配；代表性样例通过不意味着任何公式都可在任意纸张上排好。
- 未做实体手机触摸、屏幕阅读器朗读及实际打印件分页验收；本轮没有因此修改全局触摸、选字或滚动策略。
