# 对话重点划线：实现与验收

日期：2026-09-06

## 行为

- 新讲解收到 message.complete 后，以原题签名状态、该次操作、讲解范围和完整正文发起独立 /learning/emphasis 请求。正文、学习任务和输入不等待划线。
- 同一题的原始文字不会因划线而被改写。标记随现有会话存储，刷新不重新请求模型。旧消息不会被批量补标。
- 原题或讲解片段编号供模型引用。模型提供选点理由及把握度，服务端检查依据编号、原文唯一匹配、完整公式、KaTeX可解析性、数量和覆盖比例。
- 最多三处、同段最多一处、标记内容总长度不超过约32%且最高180字符。低于0.90的模型自报把握度不接受；它不是统计校准的准确率。
- 文字用可随换行分段的原生下划线（2px、绿色 #39886d、字下偏移 .2em），公式保持完整KaTeX节点并使用同色底线。无额外布局尺寸、点击事件或定位蒙层。已在当前真实会话确认：原先存在的标记因低透明度背景线不醒目，现已提高可见性，未增加标记数量。
- 请求有独立超时；浏览器最多两个标记请求在途，开始新题/卸载取消。写回还核对题目ID、消息ID、完整正文和消息状态，避免串题。失败静默降级为正常讲解，无强制重试和额外学习关卡。
- 标记结果包括内部理由，但不向学生增加“核验报告”。不会自动替学生证明推导正确。

## 真实模型样本审阅

开发期曾发现模型将逐字依据改写成说明，导致合法选点被误过滤。已改为依据片段编号，保留原文与公式严格匹配。

抽查了八种合成讲解情境。正例覆盖同一道二次方程题的三个小问、物理匀速和化学配平。参考原始输出见 outputs/emphasis/quality-samples.json 和 quality-negative-samples.json（含开发期候选及过滤结果）。

| 情境 | 抽查选点/行为 |
| --- | --- |
| 求参数范围 | 根存在条件、判别式非负及代入系数的关系 |
| 韦达变形 | 将平方和转为两根和、两根积的完整恒等式 |
| 求斜边 | 勾股关系和长度只取正值；低把握的旁支提醒在最终0.90规则下过滤 |
| 匀速运动 | 速度不随时间变化，因此可沿用同一速度 |
| 配平 | 改系数而不是改下标，保留物质种类 |
| 纯收尾提醒 | 返回空标记 |
| 不等式错误示例 | 未给错误的k≥9划线，保留正确方向 |
| 讲解与“不相等实根”条件矛盾 | 返回空标记，不为矛盾讲解作重点背书 |

真实独立HTTP接口另验证一次：本地9000端口返回200及经过校验的标记。正例样本后台判断约2.8–5.6秒、空标记样本约0.9–1.6秒，均不计入正文等待。这不是性能保证，也不代表所有题型已验收。

## 自动与浏览器验证

- npm test：817项通过。
- typecheck、lint、函数编译和静态站点构建通过。
- 初始JavaScript 741,798字节，gzip估算226,117字节，仍低于既有预算。
- node scripts/emphasis-e2e.mjs：实际浏览器+受控延迟接口；正文先出现、任务按钮可用，随后显示两处标记；出现前后消息高度与文本不变。
- 验证320/390宽度、MathML保留、选中标记文字后仍可“问一问”、刷新保留且不重复调用、接口失败降级、开始新题取消旧结果。
- 预览：outputs/emphasis/chat-390.png、chat-320.png。

## 边界

原文和结构校验不等于教学语义证明。模型仍可能误判重要程度，或因为保守过滤漏标；自报把握度不能当作准确率。该版本没有学科专家大规模盲测，也不保证任何题目都能划准。应继续积累真实使用样本、误标和漏标反馈。

超过16000字符或不足30字符的消息不调用该可选能力；图片本身不增加额外上传，仅使用本题已有的结构化题图证据。历史无标记消息、失败/未完成消息和演示模式不会被强行生成装饰性重点。
